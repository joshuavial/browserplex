# Bead browserplex-7p0.3 — DOCS

**No shipped user-facing docs change in this bead.** Rationale:

- `bp` is runnable (`node dist/cli/index.js <tool> …`) but the ergonomic command surface (grouped
  subcommands, positional args, `screenshot -o`, `fill --field`, `eval` from stdin) is bead **.4**,
  and the installable `bp` bin + README/CHANGELOG coverage is bead **.7**. Documenting the generic
  syntax now would be immediately superseded by .4's command layer.
- The MCP server surface is unchanged.

CLI/client design is recorded in `docs/plans/process/browserplex-7p0.3-PLAN.md` (internal design).
